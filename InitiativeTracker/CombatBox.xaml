<UserControl x:Class="InitiativeTracker.CombatBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InitiativeTracker"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    <Border BorderBrush="{StaticResource ActiveBorder}" BorderThickness="1">
        <Grid Margin="2" Background="{StaticResource ActiveBackground}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="50"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" HorizontalContentAlignment="Right"
                   Content="{Binding Character.Name}" FontWeight="Bold" Foreground="{StaticResource ActiveText}"/>
            <Label Grid.Row="1" HorizontalContentAlignment="Right" Content="Health"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="3"
                     Text="{Binding Health}" />
        </Grid>
    </Border>
</UserControl>
